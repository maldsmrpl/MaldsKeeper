@using MaldsKeeperWebApp.ViewModels
@model IEnumerable<IndexRecordViewModel>

<h1>Record View</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>Title</th>
            <th>Login</th>
            <th>Password</th>
            <th>Description</th>
            <th>Added Time</th>
            <th>Edited Time</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.Title)</td>
                <td>@Html.DisplayFor(modelItem => item.Login)</td>
                <td>@Html.DisplayFor(modelItem => item.Password)</td>
                <td>@Html.DisplayFor(modelItem => item.Description)</td>
                <td>@Html.DisplayFor(modelItem => item.AddedTime)</td>
                <td>@Html.DisplayFor(modelItem => item.EditedTime)</td>
                <td>
                    <a type="button" class="btn btn-sm btn-outline-info" asp-controller="Record" asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                    <a type="button" class="btn btn-sm btn-outline-info" asp-controller="Record" asp-action="Details" asp-route-id="@item.Id">Details</a> |
                    <a type="button" class="btn btn-sm btn-outline-info" asp-controller="Record" asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>